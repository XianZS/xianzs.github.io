# Python 3.14.0 http标准库完整学习文档

## 一、http标准库核心概念
### 1.1 什么是http标准库
Python的`http`标准库是**HTTP协议的原生实现**，提供了处理HTTP通信的底层基础组件。它不是一个单一的模块，而是一个包含多个子模块的包，分别负责HTTP协议的不同方面。与第三方库（如`requests`）相比，`http`库更接近协议本身，能让你深入理解HTTP的工作原理。

### 1.2 Python 3.14.0 http库的完整结构
| 子模块 | 主要功能 | 典型使用场景 |
|--------|----------|--------------|
| `http.client` | HTTP/HTTPS客户端实现 | 发送原始HTTP请求、底层网络控制 |
| `http.server` | HTTP服务器实现 | 快速搭建简单Web服务器、API服务 |
| `http.cookies` | Cookie解析与创建 | 处理HTTP请求/响应中的Cookie头 |
| `http.cookiejar` | Cookie持久化与自动管理 | 自动处理会话Cookie、跨请求保持状态 |
| `http.HTTPStatus` | HTTP状态码枚举 | 提高代码可读性、避免硬编码状态码 |

> **重要说明**：`urllib`库是基于`http`库构建的更高层次封装，而`requests`等第三方库又进一步封装了`urllib`。学习`http`标准库是理解所有Python HTTP工具的基础。

## 二、基础部分：核心模块详解与示例

### 2.1 http.HTTPStatus：状态码的优雅处理
`http.HTTPStatus`是Python 3.5引入的枚举类，在Python 3.14.0中已包含所有RFC标准定义的HTTP状态码。它解决了硬编码状态码可读性差的问题。

**示例代码：**

```python
import http

# 1. 基本使用
print(f"成功响应: {http.HTTPStatus.OK}")  # 输出: HTTPStatus.OK
print(f"状态码值: {http.HTTPStatus.NOT_FOUND.value}")  # 输出: 404
print(f"状态码短语: {http.HTTPStatus.NOT_FOUND.phrase}")  # 输出: Not Found
print(f"详细描述: {http.HTTPStatus.NOT_FOUND.description}")  # 输出: 请求的资源不存在

# 2. 状态码分类判断
status = http.HTTPStatus.CREATED
print(f"是否成功: {200 <= status.value < 300}")  # 输出: True
print(f"是否客户端错误: {400 <= status.value < 500}")  # 输出: False

# 3. 根据数值获取状态码对象
status_obj = http.HTTPStatus(403)
print(f"403对应的状态: {status_obj.name}")  # 输出: FORBIDDEN
```

### 2.2 http.client：基础HTTP客户端
`http.client`实现了HTTP/1.1协议的客户端，支持持久连接、分块传输、HTTPS等特性。它是所有Python HTTP客户端的底层基础。

#### 2.2.1 最简单的GET请求
```python
import http.client

# 1. 创建连接对象（此时并未建立实际网络连接）
conn = http.client.HTTPConnection("www.example.com", port=80, timeout=10)

try:
    # 2. 发送请求（此时才建立连接）
    conn.request("GET", "/")
    
    # 3. 获取响应对象
    response = conn.getresponse()
    
    # 4. 处理响应
    print(f"状态码: {response.status} {response.reason}")
    print("\n响应头:")
    for name, value in response.getheaders():
        print(f"{name}: {value}")
    
    # 5. 读取响应体（注意：响应体只能读取一次）
    body = response.read()
    print(f"\n响应体长度: {len(body)} 字节")
    print(body.decode("utf-8")[:300])  # 只打印前300个字符

finally:
    # 6. 关闭连接
    conn.close()
```

#### 2.2.2 HTTPS请求
HTTPS请求与HTTP请求几乎完全相同，只需使用`HTTPSConnection`类：
```python
import http.client
import ssl

# 创建HTTPS连接（默认验证服务器证书）
conn = http.client.HTTPSConnection("github.com", timeout=10)

# 如需跳过证书验证（仅用于测试！）
# context = ssl._create_unverified_context()
# conn = http.client.HTTPSConnection("github.com", context=context, timeout=10)

try:
    conn.request("GET", "/")
    response = conn.getresponse()
    print(f"HTTPS请求状态: {response.status} {response.reason}")
finally:
    conn.close()
```

#### 2.2.3 带请求头和查询参数的请求
```python
import http.client
import urllib.parse

# 构建查询参数
params = urllib.parse.urlencode({
    "q": "python http client",
    "page": 1,
    "per_page": 10
})

# 自定义请求头
headers = {
    "User-Agent": "Python http.client/3.14.0 (Learning Demo)",
    "Accept": "application/json",
    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8"
}

conn = http.client.HTTPSConnection("httpbin.org")
try:
    conn.request("GET", f"/get?{params}", headers=headers)
    response = conn.getresponse()
    print(response.read().decode("utf-8"))
finally:
    conn.close()
```

#### 2.2.4 POST请求（JSON与表单数据）
```python
import http.client
import json
import urllib.parse

conn = http.client.HTTPSConnection("httpbin.org")

try:
    # 1. 发送JSON数据
    json_data = {"name": "Python", "version": "3.14.0"}
    json_body = json.dumps(json_data)
    
    json_headers = {
        "Content-Type": "application/json",
        "Content-Length": str(len(json_body))
    }
    
    conn.request("POST", "/post", body=json_body, headers=json_headers)
    response = conn.getresponse()
    print("JSON POST响应:")
    print(response.read().decode("utf-8"))
    print("-" * 50)
    
    # 2. 发送表单数据
    form_data = urllib.parse.urlencode({
        "username": "testuser",
        "password": "testpass"
    })
    
    form_headers = {
        "Content-Type": "application/x-www-form-urlencoded",
        "Content-Length": str(len(form_data))
    }
    
    conn.request("POST", "/post", body=form_data, headers=form_headers)
    response = conn.getresponse()
    print("表单POST响应:")
    print(response.read().decode("utf-8"))

finally:
    conn.close()
```

### 2.3 http.cookies：Cookie的解析与创建
`http.cookies`模块提供了`SimpleCookie`类，专门用于处理HTTP协议中的Cookie头。

**示例代码：**
```python
import http.cookies

# 1. 创建Cookie
cookie = http.cookies.SimpleCookie()

# 设置基本Cookie
cookie["user_id"] = "12345"
cookie["username"] = "python_learner"

# 设置Cookie属性
cookie["user_id"]["expires"] = "Wed, 01 Jan 2030 00:00:00 GMT"
cookie["user_id"]["path"] = "/"
cookie["user_id"]["domain"] = ".example.com"
cookie["user_id"]["secure"] = True  # 仅HTTPS传输
cookie["user_id"]["httponly"] = True  # 禁止JavaScript访问

# 2. 输出Cookie头（用于HTTP响应）
print("Set-Cookie头:")
print(cookie.output())
print()

# 3. 解析Cookie头（用于HTTP请求）
cookie_header = "user_id=12345; username=python_learner; session_id=abcdef123456"
parsed_cookie = http.cookies.SimpleCookie(cookie_header)

print("解析后的Cookie:")
for key, morsel in parsed_cookie.items():
    print(f"{key}: {morsel.value}")
```

### 2.4 http.server：快速搭建HTTP服务器
`http.server`模块提供了构建HTTP服务器的基础类，核心是`HTTPServer`（服务器类）和`BaseHTTPRequestHandler`（请求处理类）。

#### 2.4.1 最简单的Web服务器
```python
from http.server import HTTPServer, BaseHTTPRequestHandler

class MyRequestHandler(BaseHTTPRequestHandler):
    # 处理GET请求
    def do_GET(self):
        # 设置响应状态码
        self.send_response(http.HTTPStatus.OK)
        
        # 设置响应头
        self.send_header("Content-Type", "text/html; charset=utf-8")
        self.end_headers()  # 结束响应头，必须调用
        
        # 发送响应体
        response = """
        <!DOCTYPE html>
        <html>
        <head>
            <title>Python HTTP服务器</title>
        </head>
        <body>
            <h1>Hello from Python 3.14.0 http.server!</h1>
            <p>这是一个使用标准库搭建的简单Web服务器</p>
            <p>请求路径: {}</p>
        </body>
        </html>
        """.format(self.path)
        
        self.wfile.write(response.encode("utf-8"))

if __name__ == "__main__":
    # 服务器地址（空字符串表示监听所有接口）
    server_address = ("", 8000)
    
    # 创建服务器实例
    httpd = HTTPServer(server_address, MyRequestHandler)
    
    print(f"服务器启动成功，访问地址: http://localhost:8000")
    print("按 Ctrl+C 停止服务器")
    
    try:
        # 启动服务器（永久运行）
        httpd.serve_forever()
    except KeyboardInterrupt:
        print("\n服务器已停止")
        httpd.shutdown()
```

## 三、进阶部分：高级特性与最佳实践

### 3.1 http.client高级用法
#### 3.1.1 持久连接（HTTP/1.1 Keep-Alive）
`http.client`默认支持HTTP/1.1持久连接，可以在一个TCP连接上发送多个请求，显著提高性能。

```python
import http.client

conn = http.client.HTTPSConnection("httpbin.org")

try:
    # 第一个请求
    conn.request("GET", "/get?request=1")
    response1 = conn.getresponse()
    print(f"第一个请求状态: {response1.status}")
    response1.read()  # 必须读取完响应体才能发送下一个请求
    
    # 第二个请求（复用同一个TCP连接）
    conn.request("GET", "/get?request=2")
    response2 = conn.getresponse()
    print(f"第二个请求状态: {response2.status}")
    response2.read()
    
    # 检查连接是否仍然打开
    print(f"连接是否打开: {not conn.sock}")  # 输出: True

finally:
    conn.close()
```

#### 3.1.2 分块传输编码处理
`http.client`自动处理分块传输编码（`Transfer-Encoding: chunked`），你无需手动解析分块数据。

```python
import http.client

conn = http.client.HTTPSConnection("httpbin.org")

try:
    # 这个端点会返回分块数据
    conn.request("GET", "/stream/3")
    response = conn.getresponse()
    
    print(f"状态码: {response.status}")
    print(f"是否分块传输: {response.chunked}")  # 输出: True
    
    # 逐块读取数据
    while True:
        chunk = response.read(1024)
        if not chunk:
            break
        print(f"收到块: {chunk.decode('utf-8').strip()}")

finally:
    conn.close()
```

#### 3.1.3 异常处理最佳实践
`http.client`定义了多种异常类，用于处理不同类型的HTTP错误。

```python
import http.client
import socket

try:
    # 改用稳定可访问的域名
    conn = http.client.HTTPSConnection("httpbin.org", timeout=5)
    conn.request("GET", "/")
    response = conn.getresponse()
    data = response.read()
    print("状态码:", response.status)

# ✅ 正确：捕获域名解析失败
except socket.gaierror:
    print("错误：无法解析主机名（域名错误/无网络）")

# ✅ 正确：无效URL
except http.client.InvalidURL:
    print("错误：无效的URL")

# ✅ 正确：HTTP协议错误
except http.client.HTTPException as e:
    print(f"HTTP协议错误: {e}")

# ✅ 正确：超时
except socket.timeout:
    print("错误：请求超时")

# 连接被拒绝
except ConnectionRefusedError:
    print("错误：连接被拒绝")

except Exception as e:
    print(f"未知错误: {e}")

finally:
    # 安全关闭
    try:
        conn.close()
    except:
        pass
```

### 3.2 http.cookiejar：自动Cookie管理
`http.cookiejar`模块提供了`CookieJar`类，能够自动从响应中提取Cookie，并在后续请求中自动发送，实现会话保持。

```python
import http.client
import http.cookiejar
import urllib.request

# 必须用的 CookieJar ✅
cookie_jar = http.cookiejar.CookieJar()

# 创建请求对象（兼容cookiejar）
req = urllib.request.Request("https://httpbin.org/cookies/set?name=python&version=3.14.0")
conn = http.client.HTTPSConnection("httpbin.org")

try:
    # 1. 设置Cookie
    conn.request("GET", req.full_url)
    resp = conn.getresponse()
    resp.read()

    # 2. cookiejar 提取Cookie ✅
    cookie_jar.extract_cookies(resp, req)
    print("收到的Cookie:")
    for c in cookie_jar:
        print(f"{c.name}: {c.value}")

    # 3. 从 cookiejar 中获取Cookie，手动构建请求头（解决服务器收不到的问题）
    cookie_list = [f"{c.name}={c.value}" for c in cookie_jar]
    cookie_header = "; ".join(cookie_list)

    # 关键：必须带 Host 头，httpbin 才会识别Cookie
    headers = {
        "Host": "httpbin.org",
        "Cookie": cookie_header
    }

    # 4. 发送请求
    conn.request("GET", "/cookies", headers=headers)
    resp2 = conn.getresponse()
    print("\n服务器返回的Cookie:")
    print(resp2.read().decode("utf-8"))

finally:
    conn.close()
```

### 3.3 http.server高级用法
#### 3.3.1 处理多种HTTP方法
```python
from http.server import HTTPServer, BaseHTTPRequestHandler
import json
import http

class APIServerHandler(BaseHTTPRequestHandler):
    # 辅助方法：发送JSON响应
    def send_json(self, data, status_code=http.HTTPStatus.OK):
        self.send_response(status_code)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.end_headers()
        self.wfile.write(json.dumps(data, ensure_ascii=False, indent=2).encode("utf-8"))
    
    # 辅助方法：读取请求体中的JSON数据
    def get_json_body(self):
        content_length = int(self.headers.get("Content-Length", 0))
        if content_length == 0:
            return None
        body = self.rfile.read(content_length)
        return json.loads(body.decode("utf-8"))
    
    def do_GET(self):
        if self.path == "/":
            self.send_json({"message": "欢迎使用Python API服务器", "endpoints": ["/api/users", "/api/info"]})
        elif self.path == "/api/info":
            self.send_json({
                "server": "Python http.server",
                "version": "3.14.0",
                "status": "running"
            })
        else:
            self.send_json({"error": "资源不存在"}, http.HTTPStatus.NOT_FOUND)
    
    def do_POST(self):
        if self.path == "/api/users":
            try:
                user_data = self.get_json_body()
                if not user_data or "name" not in user_data:
                    self.send_json({"error": "无效的请求数据"}, http.HTTPStatus.BAD_REQUEST)
                    return
                
                # 模拟创建用户
                user_id = 1
                new_user = {"id": user_id, "name": user_data["name"], "email": user_data.get("email", "")}
                
                self.send_json({"message": "用户创建成功", "user": new_user}, http.HTTPStatus.CREATED)
            except json.JSONDecodeError:
                self.send_json({"error": "无效的JSON格式"}, http.HTTPStatus.BAD_REQUEST)
        else:
            self.send_json({"error": "资源不存在"}, http.HTTPStatus.NOT_FOUND)

if __name__ == "__main__":
    server = HTTPServer(("", 8000), APIServerHandler)
    print("API服务器启动在 http://localhost:8000")
    server.serve_forever()
```

#### 3.3.2 静态文件服务器
`http.server`内置了`SimpleHTTPRequestHandler`，可以直接作为静态文件服务器使用：
```python
from http.server import HTTPServer, SimpleHTTPRequestHandler
import os

if __name__ == "__main__":
    # 切换到静态文件目录
    os.chdir("./static")
    
    server_address = ("", 8000)
    httpd = HTTPServer(server_address, SimpleHTTPRequestHandler)
    
    print(f"静态文件服务器启动在 http://localhost:8000")
    print(f"服务目录: {os.getcwd()}")
    httpd.serve_forever()
```

## 四、实战案例：简易HTTP文件传输系统
我们将使用`http`标准库实现一个完整的文件传输系统，包含服务器端和客户端，支持文件上传、下载、列表和删除功能。

### 4.1 文件服务器实现
```python
from http.server import HTTPServer, BaseHTTPRequestHandler
import os
import json
import http
import urllib.parse

# 文件服务器请求处理器
class FileServerHandler(BaseHTTPRequestHandler):
    # 配置：文件存储的文件夹名称
    STORAGE_DIR = "file_storage"

    def __init__(self, *args, **kwargs):
        # 程序启动时自动创建文件存储目录（不存在则创建）
        os.makedirs(self.STORAGE_DIR, exist_ok=True)
        # 调用父类构造方法
        super().__init__(*args, **kwargs)

    # ------------------- 工具方法：统一发送JSON响应 -------------------
    def send_json(self, data, status_code=http.HTTPStatus.OK):
        """
        向客户端返回JSON格式数据
        :param data: 要返回的数据（字典/列表）
        :param status_code: HTTP状态码
        """
        # 发送状态码
        self.send_response(status_code)
        # 设置响应头：数据格式为JSON
        self.send_header("Content-Type", "application/json; charset=utf-8")
        # 结束响应头
        self.end_headers()
        # 将数据转为JSON字符串并发送给客户端
        self.wfile.write(json.dumps(data, ensure_ascii=False, indent=2).encode("utf-8"))

    # ------------------- 处理GET请求（查询/下载文件） -------------------
    def do_GET(self):
        # 解析请求路径
        parsed_path = urllib.parse.urlparse(self.path)
        path = parsed_path.path

        # 1. 获取文件列表
        if path == "/files":
            files = []
            # 遍历存储目录下的所有文件
            for filename in os.listdir(self.STORAGE_DIR):
                file_path = os.path.join(self.STORAGE_DIR, filename)
                # 只统计文件，排除文件夹
                if os.path.isfile(file_path):
                    files.append({
                        "name": filename,
                        "size": os.path.getsize(file_path),
                        "modified_time": os.path.getmtime(file_path)
                    })
            self.send_json({"files": files})

        # 2. 下载文件
        elif path.startswith("/download/"):
            # 提取文件名
            filename = urllib.parse.unquote(path[len("/download/"):])
            file_path = os.path.join(self.STORAGE_DIR, filename)

            # 文件不存在则返回404
            if not os.path.isfile(file_path):
                self.send_json({"error": "文件不存在"}, http.HTTPStatus.NOT_FOUND)
                return

            # 发送文件流
            self.send_response(http.HTTPStatus.OK)
            self.send_header("Content-Type", "application/octet-stream")
            self.send_header("Content-Disposition", f"attachment; filename*=UTF-8''{urllib.parse.quote(filename)}")
            self.send_header("Content-Length", str(os.path.getsize(file_path)))
            self.end_headers()

            # 读取文件并发送
            with open(file_path, "rb") as f:
                self.wfile.write(f.read())

        # 无效路径
        else:
            self.send_json({"error": "路径不存在"}, http.HTTPStatus.NOT_FOUND)

    # ------------------- 处理POST请求（上传文件） -------------------
    def do_POST(self):
        if self.path == "/upload":
            # 从请求头获取文件名（客户端必须传递）
            filename = self.headers.get("X-Filename")
            if not filename:
                self.send_json({"error": "缺少文件名请求头"}, http.HTTPStatus.BAD_REQUEST)
                return

            # 解码文件名（处理中文）
            filename = urllib.parse.unquote(filename)
            file_path = os.path.join(self.STORAGE_DIR, filename)

            # 读取文件内容
            content_length = int(self.headers.get("Content-Length", 0))
            file_content = self.rfile.read(content_length)

            # 保存文件到本地
            with open(file_path, "wb") as f:
                f.write(file_content)

            # 返回上传成功信息
            self.send_json({
                "message": "文件上传成功",
                "filename": filename,
                "size": len(file_content)
            }, http.HTTPStatus.CREATED)
        else:
            self.send_json({"error": "路径不存在"}, http.HTTPStatus.NOT_FOUND)

    # ------------------- 处理DELETE请求（删除文件） -------------------
    def do_DELETE(self):
        parsed_path = urllib.parse.urlparse(self.path)
        if parsed_path.path.startswith("/delete/"):
            filename = urllib.parse.unquote(parsed_path.path[len("/delete/"):])
            file_path = os.path.join(self.STORAGE_DIR, filename)

            if not os.path.isfile(file_path):
                self.send_json({"error": "文件不存在"}, http.HTTPStatus.NOT_FOUND)
                return

            # 删除文件
            os.remove(file_path)
            self.send_json({"message": f"文件 {filename} 删除成功"})
        else:
            self.send_json({"error": "路径不存在"}, http.HTTPStatus.NOT_FOUND)

# ------------------- 启动服务器 -------------------
def run_server(host="0.0.0.0", port=8000):
    """
    启动文件服务器
    :param host: 监听地址 0.0.0.0 代表所有网卡
    :param port: 端口号
    """
    server = HTTPServer((host, port), FileServerHandler)
    print(f"✅ 文件服务器启动成功：http://localhost:{port}")
    print(f"📂 文件存储目录：{os.path.abspath(FileServerHandler.STORAGE_DIR)}")
    print("="*50)
    print("可用接口：")
    print("GET    /files          - 获取文件列表")
    print("GET    /download/文件名 - 下载文件")
    print("POST   /upload         - 上传文件")
    print("DELETE /delete/文件名   - 删除文件")
    print("="*50)
    # 永久运行服务器
    server.serve_forever()

if __name__ == "__main__":
    run_server()
```

### 4.2 文件客户端实现
```python
import http.client
import json
import os
import urllib.parse

# 文件客户端工具类
class FileClient:
    def __init__(self, host, port):
        """
        初始化客户端
        :param host: 服务器IP
        :param port: 服务器端口
        """
        self.host = host
        self.port = port
        # 创建HTTP连接
        self.conn = http.client.HTTPConnection(host, port, timeout=10)

    # 获取服务器文件列表
    def list_files(self):
        self.conn.request("GET", "/files")
        response = self.conn.getresponse()
        return json.loads(response.read().decode("utf-8"))

    # 上传本地文件到服务器
    def upload_file(self, local_path):
        # 校验本地文件是否存在
        if not os.path.isfile(local_path):
            raise FileNotFoundError(f"本地文件不存在：{local_path}")

        # 获取文件名
        filename = os.path.basename(local_path)
        encoded_filename = urllib.parse.quote(filename)

        # 读取文件二进制内容
        with open(local_path, "rb") as f:
            file_content = f.read()

        # 请求头（携带文件名）
        headers = {
            "X-Filename": encoded_filename,
            "Content-Length": str(len(file_content))
        }

        # 发送上传请求
        self.conn.request("POST", "/upload", body=file_content, headers=headers)
        response = self.conn.getresponse()
        return json.loads(response.read().decode("utf-8"))

    # 从服务器下载文件
    def download_file(self, filename, save_path=None):
        if save_path is None:
            save_path = filename

        encoded_filename = urllib.parse.quote(filename)
        self.conn.request("GET", f"/download/{encoded_filename}")
        response = self.conn.getresponse()

        # 下载失败返回错误信息
        if response.status != 200:
            return json.loads(response.read().decode("utf-8"))

        # 保存文件到本地
        with open(save_path, "wb") as f:
            f.write(response.read())

        return {"message": f"文件下载成功，保存为：{save_path}"}

    # 删除服务器上的文件
    def delete_file(self, filename):
        encoded_filename = urllib.parse.quote(filename)
        self.conn.request("DELETE", f"/delete/{encoded_filename}")
        response = self.conn.getresponse()
        return json.loads(response.read().decode("utf-8"))

    # 关闭连接
    def close(self):
        self.conn.close()

# ------------------- 主测试流程 -------------------
def main():
    # 连接本地文件服务器
    client = FileClient("localhost", 8000)
    # 测试用的文件名
    test_filename = "test_file.txt"

    try:
        # 1. 查看初始文件列表
        print("=== 初始文件列表 ===")
        print(json.dumps(client.list_files(), indent=2, ensure_ascii=False))

        # 2. 创建本地测试文件
        with open(test_filename, "w", encoding="utf-8") as f:
            f.write("Python http标准库实战案例\n文件传输系统测试\n")

        # 3. 上传文件
        print("\n=== 上传文件 ===")
        print(json.dumps(client.upload_file(test_filename), indent=2, ensure_ascii=False))

        # 4. 查看上传后的文件列表
        print("\n=== 上传后文件列表 ===")
        print(json.dumps(client.list_files(), indent=2, ensure_ascii=False))

        # 5. 下载文件（重命名为 下载的文件.txt）
        print("\n=== 下载文件 ===")
        print(json.dumps(client.download_file(test_filename, "下载的文件.txt"), indent=2, ensure_ascii=False))

        # 6. 删除服务器上的文件
        print("\n=== 删除文件 ===")
        print(json.dumps(client.delete_file(test_filename), indent=2, ensure_ascii=False))

        # 7. 查看删除后的文件列表
        print("\n=== 删除后文件列表 ===")
        print(json.dumps(client.list_files(), indent=2, ensure_ascii=False))

    # 捕获服务器未启动的错误
    except ConnectionRefusedError:
        print("\n❌ 错误：请先启动 file_server.py 服务器！")
    except Exception as e:
        print(f"\n❌ 运行错误：{e}")
    finally:
        # 关闭连接
        client.close()
        # 清理本地测试文件
        if os.path.exists(test_filename):
            os.remove(test_filename)

if __name__ == "__main__":
    main()
```

### 4.3 运行与测试
1. 首先运行服务器：`python file_server.py`
2. 然后在另一个终端运行客户端：`python file_client.py`
3. 客户端会自动执行完整的文件操作流程
4. 你也可以使用curl手动测试API：
   ```bash
   # 列出文件
   curl http://localhost:8000/files
   
   # 上传文件
   curl -X POST -H "X-Filename: test.txt" --data-binary @test.txt http://localhost:8000/upload
   
   # 下载文件
   curl -O http://localhost:8000/download/test.txt
   
   # 删除文件
   curl -X DELETE http://localhost:8000/delete/test.txt
   ```

## 五、总结与扩展学习
### 5.1 核心要点总结
- `http`标准库是Python处理HTTP协议的底层基础，提供了客户端、服务器、Cookie处理等完整功能
- `http.client`适合需要精细控制HTTP请求的场景，但日常开发中更推荐使用`requests`库
- `http.server`适合快速搭建简单的Web服务和API，但生产环境应使用专业的Web框架
- `http.cookies`和`http.cookiejar`提供了完整的Cookie处理能力，是实现会话管理的基础
- Python 3.14.0的`http`库完全兼容HTTP/1.1标准，支持持久连接、分块传输等现代特性

### 5.2 扩展学习方向
1. **更高层次的HTTP客户端**：学习`urllib.request`和`requests`库，它们提供了更友好的API和更强大的功能
2. **异步HTTP**：学习`aiohttp`库，用于构建高性能的异步HTTP客户端和服务器
3. **Web框架**：学习FastAPI、Flask、Django等现代Web框架，用于开发复杂的Web应用
4. **HTTP/2和HTTP/3**：了解新一代HTTP协议的特性和Python实现
5. **网络安全**：学习HTTPS、TLS、OAuth2等网络安全相关知识
